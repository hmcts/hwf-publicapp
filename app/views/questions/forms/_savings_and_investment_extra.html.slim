- if ucd_changes_apply?(@online_application.calculation_scheme)
  = render("questions/forms/savings_and_investment_extra/post_ucd", f: f)
- else
  = render("questions/forms/savings_and_investment_extra/pre_ucd", f: f)
